faster-whisper>=1.0.0
google-generativeai>=0.7.0

# --- اختیاری: فقط برای خروجی «صوت فارسی» لازم است ---
# edge-tts     : موتور تبدیل متن به گفتار (کیفیت بالا، رایگان، نیازمند اینترنت)
# pydub        : چیدن قطعات صوتی روی زمان‌بندی زیرنویس (نیازمند ffmpeg روی سیستم)
edge-tts>=6.1.0
pydub>=0.25.1
python-bidi>=0.4.2
